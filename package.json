{
  "name": "keyvid-file-uploader",
  "version": "1.0.0",
  "description": "Cloudflare Worker for uploading large video files to Google Files API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.78.12",
    "vitest": "^2.1.1"
  },
  "dependencies": {}
}
